// Package middleware provides HTTP middleware for request processing.
//
// The package includes middleware for request identification, panic recovery,
// access logging, timeouts, request size limiting and authorization.
//
// Middleware is transport-level only and does not contain business logic.
package middleware
